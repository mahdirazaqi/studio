# Compatibility Matrix

Behavior-by-behavior: what Studio keeps compatible, what it deliberately breaks, and why.

**Columns:**

- **Legacy behavior** — what the legacy render module does.
- **Studio behavior** — what Studio does.
- **Compat requirement** — must an external party (Render Worker, Telegram users,
  operators) still get the outcome they depend on?
- **Break?** — is this an intentional breaking change?
- **Reason**

---

## Worker REST API

**Implemented, Phase 7** (claim/get/state/progress/duration); **result delivery
implemented, Phase 9.** See
[../integrations/worker-api.md](../integrations/worker-api.md), ADR-0004,
ADR-0032/0033/0034/0039.

| Legacy behavior                                                                                                              | Studio behavior                                                                                                                                                                         | Compat requirement                                             | Break?                                                                                                 | Reason / classification                                                                                                                                                                      |
| ---------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Endpoints unauthenticated                                                                                                    | Every `/api/v1/worker/**` endpoint (and the Worker branch of `/api/files/[fileId]`) requires `Authorization: Bearer <WorkerApiKey secret>` (hashed lookup, Department-scoped, ADR-0040) | Worker must add a credential header, issued via `/worker-keys` | **YES (intentional)**                                                                                  | **Improved / security fix.** K1. The one unavoidable Worker change.                                                                                                                          |
| Base path `/jobs/...`, `/files`, no version                                                                                  | `/api/v1/worker/jobs/...`                                                                                                                                                               | Worker base-URL config update                                  | **YES (intentional)**                                                                                  | **Intentionally changed.** Versioned external contract (ADR-0004, ADR-0033).                                                                                                                 |
| `GET /jobs/fetch`, 404 when empty                                                                                            | `POST /api/v1/worker/jobs/next`, `204` when empty — no `GET` alias, no `404` compatibility shim                                                                                         | Worker "get next job" call                                     | **YES (intentional)** — real Worker's tolerance unconfirmed                                            | **Improved (correctness) / intentionally changed (contract).** Side-effecting GET is wrong (K21); ADR-0033.                                                                                  |
| `fetch` response fields: `id, output, title, composition, template, assets[]` with asset `{composition,layer,type,src,text}` | **Same field names**, values sourced from the Job's immutable snapshot/`JobAsset` rows; `state` and `key` added                                                                         | Worker parses these fields                                     | **NO**                                                                                                 | **Preserved**, with additive fields only.                                                                                                                                                    |
| `fetch` non-atomic (double-claim possible)                                                                                   | Atomic claim (`SELECT ... FOR UPDATE SKIP LOCKED`) — verified race-free with real concurrent HTTP requests                                                                              | Worker expects one job per call                                | **NO** (improvement, invisible to Worker)                                                              | **Improved.** K3, ADR-0029.                                                                                                                                                                  |
| `PATCH /jobs/:id/state` accepts any integer 0–9                                                                              | Accepts legacy integers **and** Studio state names; maps + validates against the state machine; an illegal transition from a known state → `422`, a lost concurrency race → `409`       | Worker sends numeric states (2/3/4 etc.)                       | **NO** for valid sequences; a Worker sending nonsense now gets a clear error instead of silent success | **Improved.** K14, ADR-0029/0033.                                                                                                                                                            |
| `PATCH /jobs/:id/progress` `{progress}` unvalidated                                                                          | Same path, `{progress: 0..100}` validated, rejected once the Job is terminal                                                                                                            | Worker reports progress                                        | **NO** for valid values                                                                                | **Improved.** Range + state check.                                                                                                                                                           |
| `PATCH /jobs/:id/duration` `{duration}` unvalidated                                                                          | Same path, renamed `{durationSeconds}`, validated ≥ 0, rejected once terminal                                                                                                           | Worker reports duration                                        | **YES (intentional)** — field renamed; confirm the Worker can send the new key                         | **Intentionally changed** (resolves OD-33's field-name question as a rename, not a dual-key compat shim).                                                                                    |
| `POST /jobs/:id/upload` multipart `.mp4`, then fire-and-forget delivery                                                      | `POST /api/v1/worker/jobs/:id/result` — **raw video bytes**, not multipart; `acceptJobResult` runs media processing + delivery **synchronously, awaited**, before responding            | Worker delivers the finished render                            | **YES (intentional)** — path, method contract (raw body vs multipart), and no early-`200` all differ   | **Improved / intentionally changed.** ADR-0039. Fixes legacy's fire-and-forget delivery (K-equivalent: a crash right after `uploadJobFile` returned could lose the whole delivery silently). |
| No idempotency on the upload endpoint — a retried upload could re-trigger delivery                                           | A duplicate result (Job no longer `RENDERING`, already has `videoFileId`) is recognized and returned as-is — no reprocessing                                                            | Worker retries the result upload on a network blip             | **NO** (improvement)                                                                                   | **Improved.** ADR-0039, docs/domain/jobs.md "Rendered result".                                                                                                                               |
| `POST /files` authenticated as a **user** (`FILE_ADD`)                                                                       | **Not implemented** — no Worker input-upload endpoint; input Files come from the Gallery, resolved at Job creation                                                                      | Worker uploads input files                                     | N/A yet                                                                                                | **Deferred.**                                                                                                                                                                                |
| No idempotency on any endpoint                                                                                               | Claim/state/progress/duration are all safe under repeated/retried requests, via the existing atomic primitives — no separate idempotency-key mechanism                                  | Worker retries on network failure                              | **NO** (improvement)                                                                                   | **Improved.** ADR-0034.                                                                                                                                                                      |
| Worker may read any job (implicitly, no auth at all)                                                                         | `GET /api/v1/worker/jobs/:id` still allows reading any Job by id — honestly documented as "no per-Worker identity to restrict by," not a new restriction                                | Worker restart/recovery reads a Job by id                      | **NO** (same effective access, now authenticated)                                                      | **Preserved (access breadth), improved (now authenticated).** ADR-0034, resolves OD-30.                                                                                                      |

## Job states & lifecycle

**Implemented, Phase 6.** See [../domain/jobs.md](../domain/jobs.md), ADR-0029.

| Legacy behavior                                                          | Studio behavior                                                                                                                     | Compat requirement                                      | Break?                                                | Reason / classification                                       |
| ------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------- | ----------------------------------------------------- | ------------------------------------------------------------- |
| 10 numeric states (2/3/6 rarely/never set by backend)                    | 8 named canonical states; legacy ints will be mapped at the future Worker API boundary (Phase 7)                                    | Worker + operators understand job progress              | **NO** (mapping preserves meaning)                    | **Intentionally changed / improved.** ADR-0013, ADR-0029.     |
| `Downloading`/`Started`/`InProgress` as distinct worker substates        | Collapsed to `RENDERING` internally; the future Worker API will still accept 2/3/4                                                  | Worker sends these                                      | **NO** at the API; internal UI shows less granularity | **Simplified.** OPEN DECISION to keep substates (unaffected). |
| Terminal states: `Uploaded`, `Cancel`; `Rendered`+ blocks retry/cancel   | `UPLOADED`, `ERROR`, `CANCELED` terminal; cancel allowed only from `QUEUED`/`CLAIMED`/`RENDERING` — **enforced**, not just intended | Operators expect the same "can't cancel a finished job" | **NO**                                                | **Improved** (now actually enforced, by the state machine).   |
| `changeStateJob` accepted any integer, no validation                     | Every transition validated against a fixed graph (`isValidTransition`); rejected otherwise                                          | Worker/operators expect consistent state                | **YES (intentional)** — closes a correctness gap      | **Improved.** ADR-0013, ADR-0029.                             |
| `fetch` non-atomic (`findOne` → `save`) — two Workers could double-claim | Atomic claim (`SELECT ... FOR UPDATE SKIP LOCKED`) — manually verified race-free                                                    | Worker claims exactly one job per call                  | **NO** (invisible improvement)                        | **Improved.** ADR-0029.                                       |
| Job deleted on retry                                                     | Job **never** deleted                                                                                                               | Operators/reports rely on job history                   | **NO break for consumers** — pure improvement         | **Improved.** ADR-0005, ADR-0031.                             |

## Retry

**Implemented, Phase 6.** See [../domain/jobs.md](../domain/jobs.md), ADR-0031.

| Legacy behavior                                                 | Studio behavior                                                                                              | Compat requirement                                             | Break?                                                               | Reason / classification                                                       |
| --------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------- | -------------------------------------------------------------------- | ----------------------------------------------------------------------------- |
| Retry deletes original, creates replacement (and could lose it) | Retry creates a **new** Job, original kept untouched, linked via `retryOfJobId`                              | Operator can retry a failed job                                | **NO break in the user-facing action**; the data model differs       | **Improved / bug fix.** ADR-0005, ADR-0031.                                   |
| Quota checked **after** delete — a failed check meant data loss | Cap checked **before** any write, inside the same transaction as the insert                                  | Operator gets a clear "cap reached" and keeps their job        | **NO break** — strictly better                                       | **Improved.**                                                                 |
| Eligibility: non-terminal (almost any state) + < 3 days         | Eligibility: `ERROR` or `CANCELED` only + `JOB_RETRY_WINDOW_DAYS` (default 3, configurable)                  | Operator retries recent failures                               | **Intentionally narrower** — retrying an active job never made sense | **Intentionally changed.** Resolves OD-02 — see [jobs.md](../domain/jobs.md). |
| `retriedCount` increments; `_retriedBy` set                     | `attemptNumber` increments; `retriedByUserId`/`retryReason` recorded; full lineage chain                     | Operator sees "this was retried"                               | **NO** (richer)                                                      | **Improved.**                                                                 |
| Retry re-copied `assets` from the deleted original's live state | Retry copies the original's immutable `snapshot`/`JobAsset` rows verbatim, never re-resolving Template/Files | Operator's retry renders what the original was meant to render | **NO break** — strictly safer                                        | **Improved (historical integrity).** ADR-0031.                                |
| No audit entry                                                  | Not yet audited (no `AuditEntry` model exists — OD-23 unaffected)                                            | —                                                              | —                                                                    | **Deferred**, not yet Preserved/Improved.                                     |

## Cancel

**Implemented, Phase 6** (single-Job cancel only — no bulk endpoint in the Jobs feature
itself; Telegram's "Cancel All", Phase 8, composes the single-Job use case in a loop —
see below and [../integrations/telegram.md](../integrations/telegram.md)).

| Legacy behavior                                                         | Studio behavior                                                                                                       | Compat requirement                         | Break?                  | Reason / classification                                                                                                                           |
| ----------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------- | ------------------------------------------ | ----------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| Bulk cancel; terminal jobs silently skipped; returns the cancelled ones | Single-Job `cancelJob`, idempotent; same eligibility rule (bulk cancel not built yet)                                 | Operator cancels a job                     | Partial — bulk deferred | **Deferred** (bulk), **Preserved** (single-job eligibility rule).                                                                                 |
| Cancel resets `progress`/`duration` to 0                                | Keeps `progress`/`durationSeconds`/the whole timeline exactly as they were at cancellation                            | Operator sees a cancelled job              | Minor display change    | **Improved.** Resolves OD-26 — historical integrity favors keeping data.                                                                          |
| Telegram "Cancel All" cancels **every** job system-wide                 | Implemented, Phase 8 — `cancelAllJobsForTelegram` is scoped to the actor's own Department, cancelable-state jobs only | Operators cancel _their department's_ jobs | **YES (intentional)**   | **Improved / security fix.** This was a serious authorization bug, not a feature. See [../integrations/telegram.md](../integrations/telegram.md). |

## Job creation & worker fetch

**Implemented, Phase 6** (creation); the fetch/claim application service is implemented
but not yet exposed over REST (Phase 7).

| Legacy behavior                                                                 | Studio behavior                                                                                         | Compat requirement                                    | Break?                             | Reason / classification                                                                            |
| ------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------- | ----------------------------------------------------- | ---------------------------------- | -------------------------------------------------------------------------------------------------- |
| No department/ownership scoping — any user with access could create/see any job | Every Job's Department is derived from its Template; USER/MANAGER act only within it, ADMIN system-wide | Operators shouldn't see/create in other departments   | **YES (intentional)**              | **Improved / security fix.** ADR-0011, closes a real isolation gap.                                |
| Template's `disabled` state was not checked by `addJob`                         | Job creation rejects a `DISABLED` or soft-deleted Template                                              | Operators can't create jobs from an unusable template | **YES (intentional)** — closes K20 | **Improved.**                                                                                      |
| A missing required asset threw a generic "not found"                            | A missing/invalid/wrong-kind/cross-department asset throws a specific, safe `business_rule` error       | Operator gets a clear reason for rejection            | **NO break** (clearer message)     | **Improved.**                                                                                      |
| `fetch`: `GET /jobs/fetch`, non-atomic, unauthenticated                         | `claimNextJob()` application service: atomic, global queue, FIFO — **not yet exposed over REST**        | Worker claims the next job                            | N/A yet                            | **Deferred** (REST exposure) — see [../integrations/worker-api.md](../integrations/worker-api.md). |
| Upload cap: global, 3/UTC-day, race-prone                                       | Same value and scope, but concurrency-safe (Postgres advisory lock)                                     | Operator sees the same daily limit                    | **NO** (same rule, now race-free)  | **Preserved (value/scope), improved (concurrency).** ADR-0030.                                     |

## Templates

**Implemented, Phase 5.** See [../domain/templates.md](../domain/templates.md), ADR-0027.

| Legacy behavior                                                                                                  | Studio behavior                                                                                                                                                                                                                      | Compat requirement                                                                     | Break?                                                        | Reason / classification                                                                                                                 |
| ---------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------- | ------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------- |
| No delete                                                                                                        | Soft delete; row kept forever, never resolvable in normal lists again                                                                                                                                                                | Historical jobs resolve their template                                                 | **NO break** (new capability)                                 | **Improved.** ADR-0006.                                                                                                                 |
| No department/workspace scoping — `FILE_VIEW`-style global visibility                                            | Every Template belongs to one Department; USER/MANAGER see/act only within it, ADMIN system-wide                                                                                                                                     | Operators shouldn't see other departments' templates                                   | **YES (intentional)**                                         | **Improved / security fix.** ADR-0011, closes the "no access filters" defect in [../legacy/known-issues.md](../legacy/known-issues.md). |
| `disabled` honored only by the Telegram picker; GraphQL `addJob` could still use a disabled template             | `status: ACTIVE\|DISABLED` and `deletedAt` are real, queryable fields any future Job-creation path must check itself — not yet enforceable end-to-end since no Job feature exists                                                    | Operators can't create jobs from a disabled/deleted template, from **any** entry point | **YES (intentional)**, once Jobs land — closes K20            | **Improved (design in place; enforcement point doesn't exist yet).** See "Template / Job contract" in domain/templates.md.              |
| `name` globally unique                                                                                           | Unique per Department among non-deleted — **implemented**, partial DB index                                                                                                                                                          | Template lookup by name                                                                | Possible break if code assumed global uniqueness              | **Intentionally changed.** ADR-0011, ADR-0027 (resolves OD-09).                                                                         |
| Full-replace edit (resend every field)                                                                           | Same shape resent on edit, but assets are diffed at the **whole-list** level (replace, not per-field patch) — simpler than legacy's Mongo overwrite, not a true field-level patch either                                             | Operators edit templates                                                               | **NO break** (equal or easier)                                | **Improved.** See "Updating Template assets" in domain/templates.md.                                                                    |
| Editing changes behavior of future jobs implicitly                                                               | Editing never touches existing jobs — enforced by construction (a future Job's own immutable snapshot, not a live Template read)                                                                                                     | Operators expect old jobs unchanged                                                    | **NO break** — improvement                                    | **Improved.** ADR-0010.                                                                                                                 |
| `TemplateAsset.type` free text (`data`/`image`/`audio`/`video`; `script` injected server-side)                   | Validated `TemplateAssetKind` enum (`DATA`/`IMAGE`/`AUDIO`/`VIDEO`); no author-visible `script` kind                                                                                                                                 | Template authoring                                                                     | Possible break for exotic legacy `type` values                | **Intentionally changed** (data quality). No production data to migrate yet.                                                            |
| `imageRatio` exact-float check, Telegram only                                                                    | Named target (`PORTRAIT_9_16`/`LANDSCAPE_16_9`/`SQUARE`/`ANY`) stored on the asset; **actual tolerance-based comparison against an uploaded image is not implemented yet** — no feature accepts an image against a Template slot yet | Image uploads that were being wrongly rejected now pass                                | Not yet applicable — no regression, no fix in effect yet      | **Deferred**, not yet Preserved/Improved. OD-14 (tolerance value) stays open; tracked for the Jobs phase.                               |
| `TemplateAsset` has no File reference of its own — a Job asset held its file's path as a plain, unchecked string | `TemplateAsset.defaultFileId` — optional, department-verified, deletion-protected reference to a Gallery File                                                                                                                        | Template authoring may reuse a Gallery File as a slot default                          | **New capability**, no legacy equivalent to preserve or break | **Improved.** ADR-0027 (resolves OD-11); see "File Gallery Integration" in domain/templates.md.                                         |
| No stable per-asset identifier rules beyond a free-text `name`; duplicates were possible                         | `key` is validated (restricted charset) and unique within a Template (`@@unique([templateId, key])`, checked client-side too)                                                                                                        | Template authoring; a future Job's "fill every slot" contract                          | **NO break** — stricter validation only                       | **Improved.** See "Stable asset identifiers" in domain/templates.md.                                                                    |

## Files

| Legacy behavior                     | Studio behavior                                                                                                                                                                | Compat requirement                    | Break?                                                                                      | Reason                              |
| ----------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------- | ------------------------------------------------------------------------------------------- | ----------------------------------- |
| Global file visibility              | Department-scoped                                                                                                                                                              | Operators browse the gallery          | **YES (intentional)**                                                                       | K7.                                 |
| No cleanup, files kept forever      | `JOB_ARTIFACT` video cleanup is a real, tested, idempotent primitive (`cleanupJobArtifacts`, Phase 9) — **not auto-triggered yet** (OD-18); Gallery Assets deletable when safe | Operators reuse gallery assets        | **NO break** for gallery assets; job artifacts _can_ become transient once a trigger exists | K12, ADR-0008, ADR-0039.            |
| Job asset = copied file path string | Snapshot metadata + FK                                                                                                                                                         | Rendered jobs point at the right file | **NO break** for the Worker (snapshot still yields a usable ref)                            | ADR-0010.                           |
| ffmpeg/convert transcode on upload  | Not implemented — no input-normalization feature exists (OD-39, unaffected by Phase 9, which only processes the render _result_, not input uploads)                            | Uploaded files usable by the Worker   | **NO transcode**                                                                            | Legacy intent unclear; K2 security. |

## Telegram

**Implemented, Phase 8.** See [../integrations/telegram.md](../integrations/telegram.md),
ADR-0035/0036/0037/0038.

| Legacy behavior                                                                         | Studio behavior                                                                                                                                                                                                                                                                                     | Compat requirement                               | Break?                                                     | Classification                                                                                                                                       |
| --------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------ | ---------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| Phone-link identity via "share contact," no OTP                                         | Same mechanism; a forwarded contact card (not the sender's own) is rejected                                                                                                                                                                                                                         | Telegram users authenticate by phone             | **NO** for a legitimate user                               | **Preserved** (mechanism), **Improved** (forwarded-contact spoofing closed).                                                                         |
| Zero/multiple phone matches: undefined-ish (`findOneAndUpdate` on first match)          | `User.phone` is `@unique` — a match is never ambiguous by construction; "no match" gets one generic message                                                                                                                                                                                         | —                                                | **NO**                                                     | **Improved.** Resolves OD-06 (ADR-0036).                                                                                                             |
| In-memory `TelegrambotDataset`/`TelegrambotJobDataset` — lost on restart, unbounded     | Durable `TelegramWizardState`, lazily TTL'd (60 min default), atomic-conditional-update safe against duplicate/racing updates                                                                                                                                                                       | Users complete multi-step flows                  | **NO break** — strictly safer                              | **Improved.** ADR-0014/ADR-0037.                                                                                                                     |
| Single Track flow: pick template → (ask upload if `_channel`) → collect slots → one Job | Preserved conceptually, minus the upload question — Studio has no YouTube delivery at all (ADR-0041), so it goes straight from picking a template into slot collection                                                                                                                              | Users create one job                             | **NO** for the outcome                                     | **Preserved** (flow), **Intentionally changed** (no delivery question at all, not just unconditional).                                               |
| Album flow: ad hoc `Audio N`/`Song N` splicing into a schema-less asset map             | The **same** typed per-slot collection loop as Single Track, repeated once per track (`advanceTrackCursor`) — no grouping entity                                                                                                                                                                    | Users create N jobs from one conversation        | **NO** for the outcome (N jobs)                            | **Intentionally changed** (implementation redesigned onto Studio's typed Template model, per Phase 8 brief §25); resolves OD-12 as independent Jobs. |
| Album: an unhandled exception mid-loop aborted every remaining `addJob`, no report      | Same stop-on-first-failure behavior, but the reply states exactly how many Jobs succeeded before the error                                                                                                                                                                                          | User knows the true outcome of a partial failure | **NO break** — clearer reporting                           | **Improved.**                                                                                                                                        |
| No confirmation before creating the Job(s)                                              | An explicit Confirm/Cancel step before the final `createJob` call(s), atomically guarded against a duplicate confirm creating two batches                                                                                                                                                           | Users don't want an accidental double-submit     | **NO break** — new safety step                             | **Improved.** ADR-0037, resolves Phase 8 brief §50–52.                                                                                               |
| List of Jobs (last 10) / detail / Retry / Cancel buttons                                | Preserved — `listDepartmentJobs`/`getJob`/`retryJob`/`cancelJob`, the unmodified dashboard use cases                                                                                                                                                                                                | Users manage jobs via Telegram                   | **NO**                                                     | **Preserved**, reusing real application services instead of duplicating query logic.                                                                 |
| **"Cancel All" cancels every non-terminal job system-wide** (no owner/scope filter)     | Cancels only the actor's own department's cancelable-state Jobs                                                                                                                                                                                                                                     | Operators cancel _their department's_ jobs       | **YES (intentional)**                                      | **Improved / security fix.** This was a real authorization bug, not a feature to preserve.                                                           |
| No permission checks anywhere on the bot surface                                        | Full role + department authorization, identical to the dashboard, on every action (Template pick, Job create/retry/cancel, Cancel All)                                                                                                                                                              | Users do only what their role allows             | **YES (intentional)**                                      | **Improved / security fix.** The single most important behavioral change in this phase.                                                              |
| Emoji-prefixed `callback_data` (`🔹`/`🔁`/`🚫`/`📜`/`📃`) coupled to button text        | Stable action codes (`tpl:s:`/`tpl:a:`/`dlv:`/`cfm:`/`job:d:`/`job:r:`/`job:c:`), re-validated server-side on every use                                                                                                                                                                             | Buttons route correctly                          | **NO break**                                               | **Improved.**                                                                                                                                        |
| File input: URL-extension MIME guess, could corrupt `File` records                      | Real content-type sniffing via the unmodified `upload-file.ts` — the same path the dashboard's own upload form uses                                                                                                                                                                                 | Uploaded media is usable by the Worker           | **NO break**                                               | **Improved.**                                                                                                                                        |
| `imageRatio` exact-float check, Telegram-only                                           | Not implemented on either surface (dashboard or Telegram) — OD-14 stays open                                                                                                                                                                                                                        | —                                                | N/A                                                        | **Removed** (the buggy legacy check itself), **Deferred** (a correct replacement).                                                                   |
| Notifications on Rendered/Uploaded/Error, all failures silently swallowed               | **Implemented, Phase 9, revised ADR-0041.** Best-effort DM on `RENDERED` (`accept-job-result.ts`) — a failure is logged, never retried, never blocks the Job, exactly matching legacy's own "swallowed" behavior. No `Uploaded`/delivery-failure notification exists — Studio has no delivery step. | Users get notified on completion                 | **NO break** — same outcome, now with a real trigger point | **Preserved** (behavior) via a **new, real trigger point** (Phase 6/7 had none). ADR-0039/0041.                                                      |

## YouTube

**Removed, Phase 12 (ADR-0041).** Studio uploaded rendered Jobs to YouTube in Phases
9–11 (`YouTubeTarget`, a `RENDERED -> DELIVERING -> UPLOADED` post-render leg, a
`DeliveryAttempt` durability ledger, a global daily upload quota). None of it remains —
Studio's current, deliberate product decision is that it does not upload rendered Jobs
to YouTube (or anywhere else) at all. A Job's lifecycle now ends at `RENDERED`. See
ADR-0041 for the full removal record; there is no `docs/integrations/youtube.md` page
anymore.

## Notifications & audit

| Legacy behavior                            | Studio behavior                                                                                                                                                                                                                                                           | Compat requirement                           | Break?                                                   | Reason                                                                                                      |
| ------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------- | -------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------- |
| `SEND_SYSTEM_MESSAGE` only on `Rendered`   | **Not implemented.** No in-app/dashboard notification center exists — only the Telegram DM (above) fires on `RENDERED`/`UPLOADED`/`ERROR`. An operator without a linked Telegram account currently learns of completion/failure only by revisiting the Job's detail page. | Operators get notified of completion/failure | Not yet applicable — no regression, no fix in effect yet | **Deferred.** No dashboard notification mechanism exists to build this on; not required by Phase 9's scope. |
| Activity log on add/edit/cancel, not retry | Audit on all privileged actions                                                                                                                                                                                                                                           | —                                            | **NO break**                                             | K19.                                                                                                        |
