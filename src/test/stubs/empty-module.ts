// Test stub. `server-only` / `client-only` are compile-time markers that throw
// under plain Node resolution; vitest aliases them here so server modules can be
// unit-tested. See vitest.config.ts.
export {};
